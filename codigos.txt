Consulta respecto a código jsx: Tengo guardado en un useState, un objeto que recibe distinto parámetros. Uno de ellos es dataWeather.iconCode.
El parámetro luego se pasa por un condicional y según su valor, se guarda en otro estado useState una ruta de una imagen. 
Para realizar esto utilicé el siguiente código y funciona sin problemas:


```jsx

    if(dataWeather.iconCode <= 299){
        setIcon('/img/icons/thunderstorm.png')
    } else if (dataWeather.iconCode >= 300 && dataWeather.iconCode <= 321){
        setIcon('/img/icons/lightrain.png')
    } else if (dataWeather.iconCode >= 500 && dataWeather.iconCode <= 531){
        setIcon('/img/icons/rain.png')
    } else if (dataWeather.iconCode >= 600 && dataWeather.iconCode <= 631){
        setIcon('/img/icons/rain.png')
    } else if (dataWeather.iconCode === 800){
        setIcon('/img/icons/sun.png')
    } else if (dataWeather.iconCode === 801){
        setIcon('/img/icons/cloud.png')
    } else if (dataWeather.iconCode === 802){
        setIcon('/img/icons/cloudy.png')
    } else if (dataWeather.iconCode === 803){
        setIcon('/img/icons/cloudy.png')
    } else {
            console.log('There is no code yet')
    }    

```

Luego quise modificarlo de la siguiente manera, cree un objeto 'iconMap' con los diferentes codigos(los números mostrados en el condicional anterior) y luego lo utilicé en la siguiente función, pero la imagen no aparece como en el condicional anterior.


```jsx

        const getIconPath = (code) => {
          const group = `${Math.floor(code / 100)}xx`;
          console.log(group);
          const icon = iconMap[group];
          console.log(icon);
          if (icon) {
            return icon;
          }
        };
        
        const icon = getIconPath(dataWeather.iconCode);
        console.log(getIconPath);
        setIcon(icon);


```

Sabes como puedo hacer que la función recién mostrada funcione y el setIcon(icon) guarde la ruta de la imagen?